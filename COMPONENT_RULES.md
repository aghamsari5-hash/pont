# COMPONENT RULES — PONT CAFE

Reusable components are required.

Examples:

Header:
One consistent implementation.

ProductCard:
Same structure everywhere.

CategoryCard:
Same visual language.

Buttons:
Use shared styles.

Forms:
Use shared validation patterns.

Do not create duplicate components with slightly different styles.