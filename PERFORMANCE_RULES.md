# PERFORMANCE RULES — PONT CAFE

Goals:
Fast menu loading on mobile.

Rules:
- Optimize images.
- Prefer WebP.
- Use lazy loading where suitable.
- Avoid unnecessary JavaScript.
- Avoid unnecessary API calls.
- Use efficient database queries.
- Use caching only when useful.

Do not add complex infrastructure.